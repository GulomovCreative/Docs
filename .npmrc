# скрипты package.json в POSIX-синтаксисе (NODE_OPTIONS=... в build) работают и на Windows
# pnpm 11 читает из .npmrc только авторизацию и реестр, остальное молча пропускает:
# при переходе на него перенести в pnpm-workspace.yaml как `shellEmulator: true`
shell-emulator=true
